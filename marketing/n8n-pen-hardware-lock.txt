# PEN HARDWARE LOCK — image_gen_buffer / GROK_Imagine

Goal: every `pen_3ml_scene` uses the same slightly elongated Palm Beach Vitality research pen.
Only label compound name + accent color change.

## Accent color rule (hard)

| Compounds | Accent color (ring + DNA + label type + dial) |
|---|---|
| Semaglutide, Tirzepatide, Retatrutide | **BLUE** (`P-SEM-001`, `P-TIR-001`, `P-RET-001`) |
| All other peptide pens | **RED** |

## Templates (public raw URLs used by edits)

Blue:
https://raw.githubusercontent.com/PalmBeach-Vitality/pep/cursor/imagine-2-0-7786/marketing/assets/pbv-research-pen-template-blue.png

Red:
https://raw.githubusercontent.com/PalmBeach-Vitality/pep/cursor/imagine-2-0-7786/marketing/assets/pbv-research-pen-template-red.png

Blobs:
https://github.com/PalmBeach-Vitality/pep/blob/cursor/imagine-2-0-7786/marketing/assets/pbv-research-pen-template-blue.png
https://github.com/PalmBeach-Vitality/pep/blob/cursor/imagine-2-0-7786/marketing/assets/pbv-research-pen-template-red.png

`marketing/n8n-grok-imagine-body-pen-edit.txt` auto-picks blue vs red template from compound id/name.

## n8n wiring (exact new node names)

After `Prep_day_variant` (or immediately before Imagine), add:

1) IF node name: `if_pen_scene`
   Condition: `{{ $('Prep_day_variant').item.json.scene_category }}` equals `pen_3ml_scene`

2) TRUE branch — HTTP Request node name: `grok_imagine_pen_edit`
   Method: POST
   URL: `https://api.x.ai/v1/images/edits`
   Auth: same xAI bearer as GROK_Imagine
   Content-Type: application/json
   Body (expression / JSON): paste from `marketing/n8n-grok-imagine-body-pen-edit.txt`

3) FALSE branch — keep existing `GROK_Imagine` (or rename keep as-is)
   URL stays: `https://api.x.ai/v1/images/generations`
   Body: paste updated `marketing/n8n-grok-imagine-body-feed.txt`

4) Merge both Imagine outputs back into the existing Save_render_URL / Buffer path.
   Merge node name: `merge_imagine_out`
   Mode: Append (or Combine by position — whichever matches your downstream)

## If you cannot add IF yet (temporary fallback)

Keep generations URL, but re-paste `marketing/n8n-grok-imagine-body-feed.txt`
(includes longer barrel + blue/red accent rule). Prefer the edits branch ASAP.

## Sheet re-import

Re-import `3-image-lab-scenes-150` so pen rows carry longer barrel + correct BLUE/RED language
in product_form_detail / product_hero / scene_brief.

## Do NOT

- Do not send spa/bathroom marble lifestyle backgrounds into edits
- Do not change vial rows (still 10mL crimp-seal only)
- Do not use blue accents on non-GLP peptide pens
- Do not use red accents on Semaglutide / Tirzepatide / Retatrutide
