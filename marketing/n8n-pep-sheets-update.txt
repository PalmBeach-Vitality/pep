# Node: sheets_update_pep (Google Sheets — Update)
# DUPLICATE from: sheets_update_creation
# After: save_pep_outputs
#
# Sheet / tab (exact): 150-pb-pep-scenes
# Operation: Update
# Column to Match On: creation_id
# Value to Match:
# {{ $('save_pep_outputs').item.json.creation_id || $('Limit').item.json.creation_id }}

# Columns to update:

last_used_at   = {{ $now.toISO() }}
times_used     = {{ Number($('Limit').item.json.times_used || 0) + 1 }}
reel_still_url = {{ $('save_pep_outputs').item.json.reel_still_url }}
video_url      = {{ $('save_pep_outputs').item.json.video_url || $('save_pep_outputs').item.json.final_video_url }}

# Optional if columns exist on the sheet later:
# final_video_url = {{ $('save_pep_outputs').item.json.final_video_url }}
# tts_audio_url   = {{ $('save_pep_outputs').item.json.tts_audio_url }}
