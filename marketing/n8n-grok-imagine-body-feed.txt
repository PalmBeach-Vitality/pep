{{ JSON.stringify({ model: 'grok-imagine-image-2.0', aspect_ratio: '1:1', n: 1, prompt: [
  'Generate a COMPLETELY DIFFERENT Palm Beach Vitality scientific social poster for today only. Do not reuse prior-day look.',
  'Square 1080x1080. Premium print design. High contrast. Distinctive.',
  'COLOR SYSTEM TODAY (must dominate): ' + String($('Prep_day_variant').item.json.daily_color_scheme || '') + '.',
  'PATTERN TODAY (must be obvious): ' + String($('Prep_day_variant').item.json.daily_pattern || '') + '.',
  'LAYOUT TODAY (must follow exactly): ' + String($('Prep_day_variant').item.json.daily_image_brief || '') + '.',
  'MOTIF TODAY (large, visible): ' + String($('Prep_day_variant').item.json.daily_visual_motif || '') + '.',
  'Weekday: ' + String($('Prep_day_variant').item.json.day_of_week || $now.weekdayLong) + '. Date mark: ' + $now.toISODate() + '.',
  'TYPOGRAPHY RULES (mandatory): Use ONLY these exact text strings. Do not invent any other words on the image.',
  'Exact headline: ' + String($('Parse_Grok').item.json.figma_headline || $('Parse_Grok').item.json.display_name || 'BPC-157') + '.',
  'Exact subhead: ' + String($('Parse_Grok').item.json.figma_subhead || '') + '.',
  'Exact bullets only: ' + [ $('Parse_Grok').item.json.figma_bullet_1, $('Parse_Grok').item.json.figma_bullet_2, $('Parse_Grok').item.json.figma_bullet_3 ].filter(Boolean).join(' | ') + '.',
  'Brand wordmark exactly: PALM BEACH VITALITY',
  'CTA exactly: VIEW LABORATORY LISTING',
  'Disclaimer exactly: FOR LABORATORY RESEARCH USE ONLY. NOT FOR HUMAN USE.',
  'Tiny date exactly: ' + $now.toISODate() + '.',
  'Do NOT invent studies, sequences, purity, grades, pathway claims, tissue/tendon/gut claims, recovery claims, dosing, or benefit language.',
  'If a bullet is missing, leave blank space — never invent replacement bullets.',
  'Motif must stay abstract/scientific geometry only. No people, syringes, needles, injections, gyms, clinics, lifestyle photography.',
  'If two weekdays were placed side by side, a viewer must instantly see different color, pattern, and composition.'
].join(' ') }) }}
