# Node: grok_imagine_reel_still — CHARACTER LOCK body (Beat A)
# POST https://api.x.ai/v1/images/edits
# Entire body = Expression (fx ON)
#
# RULE: This is an EDIT of the master Pep PNG, not a text redesign.
# Master must appear in images[0]. Prompt must say EDIT <IMAGE_0>.

={{ JSON.stringify({
  model: 'grok-imagine-image-quality',
  aspect_ratio: '9:16',
  resolution: '2k',
  n: 1,
  images: [
    {
      url: String(
        $('prep_pep_beats').item.json.pep_ref_url ||
        $('Prep_day_variant').item.json.pep_ref_url ||
        'https://files.catbox.moe/2yfdbi.jpg'
      )
    }
  ],
  prompt: [
    'EDIT <IMAGE_0> only.',
    'Keep the character in <IMAGE_0> PIXEL-IDENTITY locked: same exact Palm Beach Pep mascot — same face, eyes, smile, tongue, rosy cheeks, 10ml label art, white baseball cap with Palm Beach Vitality sunset+palm logo, silver crimp seal, glass vial proportions, liquid line, gray tube limbs, white gloves, white sneakers, outline weight, and cartoon sticker style.',
    'FORBIDDEN: redesigning Pep, new face, new hat logo, human, different bottle, screw cap, black twist cap, dropper, extra characters.',
    'ONLY CHANGE: put this SAME character into a new full-environment 9:16 background.',
    'Background beat A: ' + String($('prep_pep_beats').item.json.beat_a_brief || '') + '.',
    'Surface: ' + String($('prep_pep_beats').item.json.surface || '') + '.',
    'Lighting: ' + String($('prep_pep_beats').item.json.lighting || '') + '.',
    'Color grade: ' + String($('prep_pep_beats').item.json.color_grade || '') + '.',
    'Slight pose only (thumbs-up or settle). Pep stays mid-ground, full body visible, not macro, not void packshot.',
    'No humans. No hospitals. No doctor offices. No new on-image text. No safety placards.'
  ].join(' ')
}) }}
