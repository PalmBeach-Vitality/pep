{{ JSON.stringify({
  model: 'grok-imagine-image-quality',
  aspect_ratio: '9:16',
  resolution: '2k',
  n: 1,
  prompt: [
    'Create a museum-grade, ultra-premium Palm Beach Vitality laboratory catalog STORY poster. Photoreal print quality, razor-sharp typography, expensive art direction — not a generic AI collage.',
    'Output intent: vertical 9:16 story master at 2K. Tall hierarchy, readable mobile crop, crisp edges, clean kerning, no warped letters, no misspellings, no junk glyphs.',
    'Today product ONLY: ' + String($('Prep_day_variant').item.json.compound_id || '') + ' — ' + String($('Parse_Grok').item.json.display_name || $('Prep_day_variant').item.json.compound_name || '') + '.',
    'Catalog URL context (do not invent other products): ' + String($('Prep_day_variant').item.json.canonical_url || '') + '.',
    'Scene seed (must change composition vs other days): ' + String($('Prep_day_variant').item.json.daily_scene_seed || ($now.toISODate() + '-' + String($('Prep_day_variant').item.json.compound_id || ''))) + '.',
    'QUALITY BAR: luxury biotech / American research catalog; controlled lighting; high micro-contrast; premium paper/ink feel OR polished digital print; zero noise, zero muddy gradients, zero stock-template look.',
    'COLOR SYSTEM TODAY (must dominate palette, light, and accents): ' + String($('Prep_day_variant').item.json.daily_color_scheme || 'deep navy, electric teal, white') + '.',
    'PATTERN TODAY (subtle, refined — never cluttered): ' + String($('Prep_day_variant').item.json.daily_pattern || 'faint blueprint grid') + '.',
    'LAYOUT TODAY (adapt to tall 9:16, follow intent): ' + String($('Prep_day_variant').item.json.daily_image_brief || 'hero compound name dominant in upper third, motif lower') + '.',
    'MOTIF TODAY (elegant scientific geometry only): ' + String($('Prep_day_variant').item.json.daily_visual_motif || 'translucent molecular lattice') + '.',
    'Image type today: ' + String($('Prep_day_variant').item.json.daily_image_type || 'premium catalog story poster') + '.',
    'Text density today: ' + String($('Prep_day_variant').item.json.daily_text_density || 'medium — clear hierarchy, generous spacing') + '.',
    'TYPOGRAPHY — use ONLY these exact strings. Perfect spelling. No extra words.',
    'Exact headline: ' + String($('Parse_Grok').item.json.figma_headline || $('Parse_Grok').item.json.display_name || '') + '.',
    'Exact subhead: ' + String($('Parse_Grok').item.json.figma_subhead || '') + '.',
    'Exact bullets only: ' + [ $('Parse_Grok').item.json.figma_bullet_1, $('Parse_Grok').item.json.figma_bullet_2, $('Parse_Grok').item.json.figma_bullet_3 ].filter(Boolean).join(' | ') + '.',
    'Brand wordmark exactly: PALM BEACH VITALITY',
    'CTA exactly: VIEW LABORATORY LISTING',
    'Footer exactly: FOR LABORATORY RESEARCH USE ONLY. NOT FOR HUMAN USE.',
    'Tiny date exactly: ' + $now.toISODate() + '.',
    'Optional purity line (only if space allows, exact): 99.99% PURITY — PEPTIDE SYNTHESIS',
    'If a bullet is missing, leave blank space — never invent replacement text.',
    'HARD BAN: people, faces, hands, syringes, needles, injectors, writing pens, clinics, gyms, lifestyle, before/after, emojis, cartoon mascots, purple neon glow spam, warped text, fake logos.',
    'Do not invent studies, other purity numbers, FDA approvals, dosing, or human-use claims.',
    'Final look: the most expensive research story frame of the week — unmistakably about today\'s compound only.'
  ].join(' ')
}) }}
